# In production, set this to your deployed Django API's URL, e.g.
# https://freshcrate-api.up.railway.app/api
# Leave unset for local dev — vite.config.ts proxies /api to localhost:8000.
VITE_API_URL=
